{
    "name": "diagnostic-detective-game",
    "version": "1.0.0",
    "description": "Diagnostic challenge game for dentists",
    "main": "app.js",
    "scripts": {
        "dev": "netlify dev",
        "build": "echo 'No build step required'",
        "deploy": "netlify deploy --prod"
    },
    "dependencies": {
        "@supabase/supabase-js": "^2.39.0"
    },
    "devDependencies": {
        "netlify-cli": "^17.0.0"
    }
}